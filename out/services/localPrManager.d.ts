import * as vscode from 'vscode';
import { BranchReview, LocalPr, ReviewMode, ReviewSourceTarget, UncommittedReview } from '../types';
import { GitService } from '../git/gitService';
export declare class LocalPrManager {
    private readonly gitService;
    private registry;
    private readonly storageDir;
    private readonly reviewsDir;
    private readonly registryPath;
    private readonly pendingReviewCreations;
    /** Destructive operations invalidate older in-flight review creations. */
    private creationEpoch;
    private readonly _onDidChange;
    readonly onDidChange: vscode.Event<void>;
    constructor(gitService: GitService, workspaceRoot: string);
    private loadRegistry;
    private saveRegistry;
    createBranchReview(baseBranch: string, targetBranch: string, activate?: boolean): Promise<BranchReview>;
    createUncommittedReview(branch: string, activate?: boolean): Promise<UncommittedReview>;
    createReview(sourceBranch: string, targetBranch: string, mode: 'branch'): Promise<BranchReview>;
    createReview(sourceBranch: string, targetBranch: string, mode: 'uncommitted'): Promise<UncommittedReview>;
    private createReviewInternal;
    private resolveAndStoreReview;
    private findReviewByIdentity;
    getPreferredBaseBranch(): string | undefined;
    setPreferredBaseBranch(branch: string): void;
    getActiveMode(): ReviewMode;
    setActiveMode(mode: ReviewMode): void;
    getReviewMode(review: LocalPr): ReviewMode;
    isUncommittedReview(review?: LocalPr): review is UncommittedReview;
    getReviewSourceTarget(review: LocalPr): ReviewSourceTarget;
    updateBranchReviewFallbackCommits(reviewId: string, sourceCommit: string, targetCommit: string): boolean;
    private invalidatePendingCreations;
    deleteReview(id: string): void;
    clearActiveReview(): boolean;
    clearAllReviews(): void;
    setActiveReview(id: string): void;
    private activateReview;
    getActiveReview(): LocalPr | undefined;
    /** Clear only the active pointer; saved reviews and comments remain intact. */
    deactivateReview(): boolean;
    listReviews(): LocalPr[];
    getReviewById(id: string): LocalPr | undefined;
    findReviewByBranch(branch: string, mode?: ReviewMode): LocalPr | undefined;
    getReviewDir(review: LocalPr): string;
    getCommentsFilePath(review: LocalPr): string;
    getReviewedFiles(): string[];
    setReviewedFiles(files: string[]): void;
    dispose(): void;
}
